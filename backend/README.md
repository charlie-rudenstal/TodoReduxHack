# Tictail Hack Backend

## Installation
Run this command to install the necessary dependencies
    
    pip install -r requirements.txt
   
After that you should be set to go and able to start the server 

    python server.py